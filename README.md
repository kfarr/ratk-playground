# ratk-playground
 playing around with reality accelerator toolkit

## running locally
`npm install`
`npm run serve`

## deploying
`npm run build`
`cd dist`
(then distribute the contents of the dist dir)
